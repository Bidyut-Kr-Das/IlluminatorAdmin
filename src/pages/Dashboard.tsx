import axios from "axios";

const Dashboard = () => {
  const logout = () => {
    // axios.post();
  };
  return (
    <main>
      <button onClick={logout}>Logout</button>
    </main>
  );
};

export default Dashboard;
